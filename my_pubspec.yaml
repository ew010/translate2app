name: ai_translator
description: AI Translator powered by llama
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^8.0.0
  # 替换为专门针对 Android 优化的 GPU 加速版
  llama_flutter_android: any

flutter:
  uses-material-design: true
