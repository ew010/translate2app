name: ai_translator
description: AI Translator powered by llama
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^8.0.0
  permission_handler: any
  # 关键：换回这个最老实、绝不挑 GPU 的纯 CPU 插件
  llama_cpp_dart: any 

flutter:
  uses-material-design: true
